'use strict'
const test = require('tape')
const proxy = require('../')
const BriskyServer = require('../')
const http = require('http')

test('brisky server - serve a build', (t) => {
  t.plan(2)
  // test slack, travis and getting files


  t.end()
})
